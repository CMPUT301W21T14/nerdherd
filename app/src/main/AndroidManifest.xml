<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nerdherd"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NerdHerd">
        <activity android:name=".ViewTrialActivity"
            android:parentActivityName=".ExperimentViewActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".StartTrialActivity"
            android:parentActivityName=".ExperimentViewActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".QRCodeActivity" />
        <activity android:name=".QuestionViewActivity" />
        <activity android:name=".QuestionsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".MySubscriptionActivity" />
        <activity android:name=".MyExperimentsActivity" />
        <activity android:name=".ExperimentViewActivity" />
        <activity android:name=".statsactivity_checking" />
        <activity android:name=".CreateExperimentActivity" />
        <activity android:name=".SearchUserActivity" />
        <activity android:name=".Reseter" />
        <activity android:name=".AvatarPicker" />
        <activity android:name=".SearchExperimentActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TrialActivity" />
        <activity android:name=".statsActivity" />
        <activity android:name=".TrialsPlot" />
        <activity android:name=".TrialsHistogram" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="12451000" />

    </application>

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!--- This might break everything but allows to not drop minSdk version to use-->
    <uses-sdk tools:overrideLibrary="androidx.camera.view, androidx.camera.camera2, androidx.camera.lifecycle, androidx.camera.core"/>
</manifest>