QR Codes selectively scan based on where you generate them from (Can be fussy) Barcodes are especially fussy.
The QR Codes genearted by the app should be scannable

Within SplashScreen.java, setting 'useMockData=true' in the InitializeData(boolean useMockData) will use a mock database with generated data if there are issues testing